ssh
===

# Terminate Frozen Session Actively

Key Sequence: Press `Enter`, then type `~.` (tilde followed by a period).

Nested Sessions: If you have nested SSH sessions (SSHing from one server into another), use more tildes. For example, `Enter` `~~.` will exit the second (inner) session while keeping the first one active.
